version: '3'

services:
  ubuntu-geth:
    image: ethereum-geth-vm
    tty: true
    working_dir: /srv
    ports:
      - "30303:30303"
      - "8545:8545"
    volumes:
      - "/home/ethereum:/srv"
    command: /bin/bash
    # network_mode: "host"
  front-end:
    image: ethereum-geth-vm
    tty: true
    working_dir: /srv
    ports:
      - "9000:9000"
    volumes:
      - "/home/ethereum:/srv"
    command: /bin/bash

